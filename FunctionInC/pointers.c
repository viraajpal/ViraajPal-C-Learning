//Single Pointer


#include<stdio.h>

int main(){
    int a = 5;
    int* x = &a; // Pointer is declare by using (int*).
    // printf("%p", &a); // output as address of a.
    // printf("%p", x); // This will give an output as address of a.
    // printf("%p", &x); // This will give an output as address of x.
    // printf("%p", *x); // x ander jiss bhi variabe ka address store hai usko point 
    // kro aur uski value print kra do .
    
    // printf("%d", a);
    return 0;
}